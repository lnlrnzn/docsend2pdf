{
  "name": "docsend-scraper-mcp",
  "version": "1.0.0",
  "description": "MCP server to convert DocSend documents to PDF",
  "type": "module",
  "bin": {
    "docsend-scraper-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "postinstall": "npx playwright install chromium --with-deps 2>/dev/null || npx playwright install chromium || echo 'Run: npx playwright install chromium'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "playwright": "^1.52.0",
    "pdf-lib": "^1.17.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
